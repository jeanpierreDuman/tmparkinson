{% extends 'base.html.twig' %}

{% block title %}Stock{% endblock %}

{% block body %}
    {{ form_start(form) }}
        {{ form_widget(form.quantity) }}
        {{ form_rest(form) }}
        <input type="submit" class="btn btn-sm btn-primary" value="Editer">
    {{ form_end(form) }}

{% endblock %}
