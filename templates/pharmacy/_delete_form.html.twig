<form method="post" class="float-end" action="{{ path('app_pharmacy_delete', {'id': pharmacy.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ pharmacy.id) }}">
    <button class="btn btn-sm btn-danger">Supprimer</button>
</form>
