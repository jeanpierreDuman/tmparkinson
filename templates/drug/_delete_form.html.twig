<form method="post" class="float-end" action="{{ path('app_drug_delete', {'id': drug.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ drug.id) }}">
    <button class="btn btn-sm btn-danger">Supprimer</button>
</form>
