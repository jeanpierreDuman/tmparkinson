{% extends 'base.html.twig' %}

{% block title %}Drug{% endblock %}

{% block body %}
    <h1>Drug</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ drug.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ drug.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_drug_index') }}">back to list</a>

    <a href="{{ path('app_drug_edit', {'id': drug.id}) }}">edit</a>

    {{ include('drug/_delete_form.html.twig') }}
{% endblock %}
