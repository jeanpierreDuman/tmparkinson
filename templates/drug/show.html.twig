{% extends 'base.html.twig' %}

{% block title %}Visualisation médicament{% endblock %}

{% block body %}
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ drug.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ drug.name }}</td>
            </tr>
        </tbody>
    </table>

    <div>
        <a class="btn btn-sm btn-primary float-start mt-4" href="{{ path('app_drug_edit', {'id': drug.id}) }}">edit</a>
    </div>

    <div class="mt-4">
        <a href="{{ path('app_drug_index') }}" class="btn btn-sm btn-warning float-start">back to list</a>
        {{ include('drug/_delete_form.html.twig') }}
    </div>
{% endblock %}
