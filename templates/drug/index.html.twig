{% extends 'base.html.twig' %}

{% block title %}Liste des médicaments{% endblock %}

{% block body %}
    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for drug in drugs %}
            <tr>
                <td>{{ drug }}</td>
                <td>
                    <a class="btn btn-sm btn-info" href="{{ path('app_drug_show', {'id': drug.id}) }}">voir</a>
                    <a class="btn btn-sm btn-warning" href="{{ path('app_drug_edit', {'id': drug.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <div>
        <a href="{{ path('app_drug_new') }}" class="btn btn-sm btn-primary">Nouveau</a>
    </div>
{% endblock %}
